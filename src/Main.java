public class Main {
    public static void main(String[] args) {


        // ! PARA TESTES ONLY
        // ! DELETE THIS LATER



        /*
        Comunication.Server.Product temp = new Comunication.Server.Product("nao leite", 124.64F, "Loja do nao leite");
        Credentials tempClient = new Credentials("main", "mainPW");

        String path = System.getProperty("user.dir") + "\\src\\Files\\";
        String usersFileName = "credentials.txt";
        String productsFileName = "products.txt";

        FileManager productManager = new FileManager(path, productsFileName);
        FileManager userManager = new FileManager(path, usersFileName);

        ArrayList<Comunication.Server.Product> list = Comunication.Server.Product.listFromFile(productManager.getListFromFile());

        for (Comunication.Server.Product p : list)
            System.out.println(p);

        list.add(temp);
        System.out.println("Produto adicionado a lista");

        productManager.appendToFile(temp.toFileString());
        System.out.println("FROM MAIN: Ficheiro alterado com sucesso");


        System.out.println("---------- CLIENTES ---------");

        ArrayList<Credentials> clients = Credentials.listFromFile(userManager.getListFromFile());

        for (Credentials c : clients)
            System.out.println(c);

        list.add(temp);
        System.out.println("Cliente adicionado a lista");

        userManager.appendToFile(tempClient.toFileString());
        System.out.println("FROM MAIN: Ficheiro alterado com sucesso");
        */

    }
}